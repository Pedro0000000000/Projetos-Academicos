#include <stdio.h> 

int main () {
    // A Torre será representada pelo ( i ) nesse codigo
    int i = 0;
    while (i <= 5) {
        printf("Movimento da Torre para direita: %d\n", i);
        i++;
    }
    
    
}